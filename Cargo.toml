[package]
name = "generative-scoped-tls"
version = "0.1.0"
edition = "2024"
description = "synchronous scoped TLS with generative bare references"
license = "MIT"

[dependencies]
generativity = "1.2.1"
